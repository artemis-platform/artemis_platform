#!/bin/bash

# source /app/config

# TODO: create `/app/config/.env` file if it does not exist

cd /app/source
  mix ecto.create --quiet
  mix ecto.migrate
  mix run apps/artemis/priv/repo/seeds.exs

/app/release/bin/artemis foreground
